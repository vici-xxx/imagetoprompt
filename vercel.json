{
  "github": {
    "silent": true
  },
  "buildCommand": "cd apps/nextjs && SKIP_ENV_VALIDATION=true bun run build",
  "outputDirectory": "apps/nextjs/.next",
  "installCommand": "bun install",
  "framework": "nextjs",
  "rootDirectory": "apps/nextjs",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  }
}
